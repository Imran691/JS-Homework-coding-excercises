// cars

function infoAboutCar (manufacturer, modelName) {
    console.log(manufacturer + " " + modelName);
    
    let car = {
        color: "White",
        transmission: "Auto",
    }
    console.log(car);
}

infoAboutCar("Honda", "City");