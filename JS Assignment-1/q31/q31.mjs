//no users

let userNames = ["admin", "Ali", "Gia", "Ayra", "Zaid"];


if (userNames === []) {
    console.log("We need to find some users.");
}

userNames = [];
if (userNames = []) {
    console.log("We need to find some users.");
}

